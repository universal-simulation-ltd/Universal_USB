/// <reference types="vite/client" />

// Injected by vite.config.ts from package.json. Declared here so the About
// dialog can state which build you are running.
declare const __APP_VERSION__: string
